version: "3.8"

services:
  civicstack:
    build:
      context: ./weavr_stacks/civicstack
      dockerfile: Dockerfile
    ports:
      - "11434:8000"  # ⬅ This is what allows external access
    container_name: civicstack
    restart: always
