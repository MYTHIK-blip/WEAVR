version: "3.8"

services:
  civicstack:
    build:
      context: ./weavr_stacks/civicstack
      dockerfile: Dockerfile
    image: civicstack:latest
    container_name: civicstack
    ports:
      - "4343:8000"  # Required external:internal mapping
    restart: always
