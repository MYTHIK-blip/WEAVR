version: '3.8'

services:
  civicstack:
    build:
      context: .
    container_name: civicstack
    image: civicstack:latest
    ports:
      - "4343:8000"  # HOST:CONTAINER — adjust if needed
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
